const impuestos = {
    impuesto1: 234,
    impuesto2: 341,
    impuesto3: 4611,
    impuesto4: 111,
}

let arrayParesKeyValues = Object.entries(impuestos)
let arrayDeKeys         = Object.keys(impuestos) 
let valores             = Object.values(impuestos) 
console.log(valores)

// async await -> swgar syntax Promise

/// 